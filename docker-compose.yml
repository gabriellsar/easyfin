services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: easyfin
      POSTGRES_USER: easyfin
      POSTGRES_PASSWORD: easyfin
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
